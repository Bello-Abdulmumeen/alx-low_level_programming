#include <stdio.h>
/**
 * main - A programme that print a line suing the printf function
 * Return: 0 (Success)
*/
int main(void)
{
pringf("with proper grammar, but the outcome is a peice of art,\n")
	return (0);
}
